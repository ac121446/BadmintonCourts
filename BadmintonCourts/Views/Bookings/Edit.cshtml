@model BadmintonCourts.Models.Booking

@{
    ViewData["Title"] = "Edit Booking";
}

<h1 class="text-center mt-5">@ViewData["Title"]</h1>

<div class="center-container">
    <div class="center-inner" style="max-width: 900px;">

        <form asp-action="Edit" method="post">
            <h2 class="text-center mb-4">Edit booking details</h2>
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

            <input type="hidden" asp-for="BookingID" />

            <div class="row g-3">
                @if (User.IsInRole("Admin"))
                {
                    <div class="col-md-6">
                        <div class="form-floating">
                            <select asp-for="BadmintonCourtsUserId" class="form-select" asp-items="ViewBag.BadmintonCourtsUserId"></select>
                            <label asp-for="BadmintonCourtsUserId">User</label>
                            <span asp-validation-for="BadmintonCourtsUserId" class="text-danger"></span>
                        </div>
                    </div>
                }

                <div class="col-md-6">
                    <div class="form-floating">
                        <select asp-for="CourtID" class="form-select" asp-items="ViewBag.CourtID"></select>
                        <label asp-for="CourtID">Court</label>
                        <span asp-validation-for="CourtID" class="text-danger"></span>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-floating">
                        <select asp-for="EquipmentID" class="form-select" asp-items="ViewBag.EquipmentID"></select>
                        <label asp-for="EquipmentID">Equipment</label>
                        <span asp-validation-for="EquipmentID" class="text-danger"></span>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-floating">
                        <input asp-for="BookingDate" class="form-control" type="date" min="@DateTime.Now.ToString("yyyy-MM-dd")" />
                        <label asp-for="BookingDate">Booking Date</label>
                        <span asp-validation-for="BookingDate" class="text-danger"></span>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-floating">
                        <input asp-for="StartTime" class="form-control" type="time" />
                        <label asp-for="StartTime">Start Time</label>
                        <span asp-validation-for="StartTime" class="text-danger"></span>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-floating">
                        <input asp-for="EndTime" class="form-control" type="time" />
                        <label asp-for="EndTime">End Time</label>
                        <span asp-validation-for="EndTime" class="text-danger"></span>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-floating">
                        <input asp-for="TotalPrice" class="form-control" />
                        <label asp-for="TotalPrice">Total Price ($)</label>
                        <span asp-validation-for="TotalPrice" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="mt-4">
                <button type="submit" class="w-100 btn btn-lg btn-success">Save Booking</button>
                <a asp-action="Index" class="w-100 btn btn-lg btn-secondary mt-2">
                    <i class="bi bi-arrow-left-circle me-1"></i> Back to List
                </a>
            </div>
        </form>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
